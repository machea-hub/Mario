const size = {
    sm: "390px",
    md: "768px",
    bg: "1024px"
}

const breakpoints = {
    bg: `(max-width: ${size.bg})`,
    md: `(max-width: ${size.md})`,
    sm: `(max-width: ${size.sm})`
}

export default breakpoints;